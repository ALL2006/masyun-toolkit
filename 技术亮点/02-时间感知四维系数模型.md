# 02 - 时间感知四维系数模型 ⏰

> **难度**: ⭐⭐⭐⭐ | **价值**: 🔥🔥🔥🔥 | **创新度**: ⭐⭐⭐⭐

---

## 📌 技术概述

### 核心创新

传统的模拟数据是**静态的**，不随时间变化。而我们创新性地设计了**四维时间系数模型**，让模拟数据能够：

- 🌅 随**一天中的小时**动态变化（早中晚客流不同）
- 📅 区分**工作日和周末**（周六客流是工作日的1.35倍）
- 🌺 适应**不同季节**（暑期客流是平时的1.25倍）
- 🌤️ 响应**天气变化**（雨天客流只有晴天的50%）

### 数学模型

```python
综合时间系数 = 小时系数 × 周末系数 × 季节系数 × 天气系数

# 实际客流量 = 基础客流量 × 综合时间系数 × 随机波动(±5%)
```

---

## 🔍 技术原理

### 四维时间系数详解

#### 1. 小时系数 (Hour Factor)

**规律**: 一天中客流呈现双峰分布

| 时段 | 系数 | 说明 |
|-----|:----:|------|
| 10:00-16:00 | 1.3-1.5 | 白天高峰期，游客最多 |
| 08:00-09:00 | 1.1-1.2 | 上午次高峰 |
| 17:00-18:00 | 1.1-1.2 | 傍晚次高峰 |
| 06:00-07:00 | 0.7 | 早晨起床时间 |
| 19:00-21:00 | 0.7-0.9 | 晚间活动 |
| 22:00-05:00 | 0.5 | 深夜凌晨 |

**代码实现**:
```python
def get_hour_factor(self) -> float:
    hour = self.now.hour

    if 10 <= hour <= 16:
        return 1.3 + (hour - 10) * 0.05  # 1.3-1.5
    elif 8 <= hour <= 9:
        return 1.1 + (hour - 8) * 0.05   # 1.1-1.15
    elif 17 <= hour <= 18:
        return 1.2 - (hour - 17) * 0.1   # 1.2-1.1
    elif 6 <= hour <= 7:
        return 0.7
    elif 19 <= hour <= 21:
        return 0.9 - (hour - 19) * 0.1
    else:
        return 0.5
```

#### 2. 周末系数 (Weekend Factor)

**规律**: 周六客流最大，周日的次之

| 星期 | 系数 | 说明 |
|:----:|:----:|------|
| 周六 | 1.35 | 一周中客流最大 |
| 周日 | 1.25 | 第二天要上班，略低 |
| 工作日 | 1.0 | 基准水平 |

**代码实现**:
```python
def get_weekend_factor(self) -> float:
    weekday = self.now.weekday()  # 0=周一, 5=周六, 6=周日

    if weekday == 5:  # 周六
        return 1.35
    elif weekday == 6:  # 周日
        return 1.25
    else:  # 工作日
        return 1.0
```

#### 3. 季节系数 (Season Factor)

**规律**: 暑期和春秋季是旅游旺季

| 月份 | 系数 | 说明 |
|:----:|:----:|------|
| 7-8月 | 1.25 | 暑期高峰，学生放假 |
| 4-5月 | 1.15 | 春季最佳旅游季节 |
| 9-10月 | 1.15 | 秋季最佳旅游季节 |
| 12-2月 | 0.8 | 冬季淡季 |
| 其他 | 1.0 | 常规月份 |

**代码实现**:
```python
def get_season_factor(self) -> float:
    month = self.now.month

    if month in [7, 8]:
        return 1.25  # 暑期
    elif month in [4, 5, 9, 10]:
        return 1.15  # 春秋季
    elif month in [12, 1, 2]:
        return 0.8   # 冬季
    else:
        return 1.0
```

#### 4. 天气系数 (Weather Factor)

**规律**: 天气越差，客流越少

| 天气 | 系数 | 说明 |
|:----:|:----:|------|
| 晴/多云 | 1.0 | 最佳天气 |
| 阴 | 0.9 | 略有影响 |
| 小雨 | 0.7 | 明显减少 |
| 中雨及以上 | 0.5 | 大幅减少 |

**代码实现**:
```python
def get_weather_factor(self, condition: str = "晴") -> float:
    if condition in ["晴", "多云"]:
        return 1.0
    elif condition == "阴":
        return 0.9
    elif condition == "小雨":
        return 0.7
    elif condition in ["中雨", "大雨", "暴雨", "雪"]:
        return 0.5
    else:
        return 1.0
```

---

## 💡 项目应用

### 代码位置

**主实现文件**: [backend/app/services/mock_service.py:14-179](../../backend/app/services/mock_service.py#L14-L179)

### 核心类: TimeAwareGenerator

```python
class TimeAwareGenerator:
    """时间感知数据生成器 - 让数据随时间动态变化"""

    def __init__(self):
        self.now = datetime.now()  # 当前系统时间

    def get_combined_factor(self, weather: str = "晴") -> float:
        """计算综合时间系数"""
        hour_factor = self.get_hour_factor()
        weekend_factor = self.get_weekend_factor()
        season_factor = self.get_season_factor()
        weather_factor = self.get_weather_factor(weather)

        # 综合系数 = 各系数相乘
        combined = hour_factor * weekend_factor * season_factor * weather_factor

        # 添加随机波动(±5%)
        random_variation = 1.0 + random.uniform(-0.05, 0.05)

        return round(combined * random_variation, 3)
```

### 应用场景1: 客流量计算

```python
def get_visitor_count(self, base: int, weather: str = "晴") -> int:
    """获取当前时间客流量"""
    combined_factor = self.get_combined_factor(weather)

    # 实际客流量 = 基础值 × 综合系数
    return int(base * combined_factor)
```

**示例计算**:
```
基础客流量: 2000人
当前时间: 周六 14:00（7月，晴天）

小时系数: 1.4 (14:00处于高峰时段)
周末系数: 1.35 (周六)
季节系数: 1.25 (7月暑期)
天气系数: 1.0 (晴天)

综合系数 = 1.4 × 1.35 × 1.25 × 1.0 = 2.3625
实际客流量 = 2000 × 2.3625 = 4725人
```

### 应用场景2: 景点实时客流

```python
def get_realtime_visitors(self, attraction_name: str) -> dict:
    """获取景点实时客流量"""
    base_visitors = ATTRACTION_BASE[attraction_name]  # 基础值
    weather = self.get_current_weather()["condition"]

    current = self.get_visitor_count(base_visitors, weather)

    return {
        "attraction": attraction_name,
        "current_visitors": current,
        "base_visitors": base_visitors,
        "combined_factor": self.get_combined_factor(weather),
        "is_peak_hour": self.is_peak_hour()
    }
```

### 应用场景3: 预警触发

```python
def check_alerts(self) -> list:
    """检查是否需要预警"""
    alerts = []
    weather = self.get_current_weather()

    # 客流超载预警
    if self.is_peak_hour() and self.is_weekend():
        visitors = self.get_visitor_count(10000, weather["condition"])
        if visitors > 15000:
            alerts.append({
                "type": "客流超载",
                "level": "high",
                "message": f"当前客流量{visitors}人，超过警戒值"
            })

    # 恶劣天气预警
    if weather["condition"] in ["中雨", "大雨", "暴雨"]:
        alerts.append({
            "type": "恶劣天气",
            "level": "medium",
            "message": f"当前{weather['condition']}，建议游客调整行程"
        })

    return alerts
```

### 前端实时展示

**文件**: [frontend/src/pages/tourist/TouristAttractions.tsx](../../frontend/src/pages/tourist/TouristAttractions.tsx)

```typescript
// 实时客流量显示
<Statistic
  title="实时客流量"
  value={attraction.currentVisitors}
  suffix="人"
  valueStyle={{ color: attraction.isPeakHour ? '#cf1322' : '#3f8600' }}
/>

// 显示时间系数信息
<Alert
  message={`当前时间系数: ${attraction.combinedFactor}`}
  description={`小时系数: ${attraction.hourFactor} | 周末系数: ${attraction.weekendFactor}`}
  type="info"
/>
```

---

## 🎯 演示话术

### 开场介绍（30秒）

```
"现在介绍我们的第二大创新——时间感知四维系数模型。

传统的模拟数据是静态的，不会随时间变化。
而我们的系统会根据当前的真实时间，动态调整数据。
请看大屏幕上的时间。"
```

### 演示互动（2分钟）

```
（在电脑上调出系统时间）

"各位评委请看，现在是（显示当前时间）。

我们可以看到几个关键数据：

① 小时系数：{current_hour_factor}
   如果现在是下午2点，处于一天中的高峰时段，
   所以系数是1.4，表示客流量是平时的1.4倍。

② 周末系数：{current_weekend_factor}
   如果今天是周六，系数是1.35，
   周末的客流量比工作日多35%。

③ 季节系数：{current_season_factor}
   现在是{current_month}月，
   {season_description}。

④ 天气系数：{current_weather_factor}
   今天是{current_weather}天气，
   {weather_impact}。

综合系数 = {hour_factor} × {weekend_factor} × {season_factor} × {weather_factor}
           = {combined_factor}

这意味着当前的客流量是基础值的{combined_factor}倍！"
```

### 动态演示（1分钟）

```
（调整系统时间，观察数据变化）

"我们可以看到，当我调整系统时间时：
- 从早上8点改到下午2点，客流量增加了{increase}%
- 从工作日改到周六，客流量增加了{increase}%
- 从晴天改到雨天，客流量减少了{decrease}%

这完全模拟了真实场景中的客流变化规律！"
```

### 总结强调（30秒）

```
"这个四维时间系数模型的价值在于：

① 高度仿真：四个维度综合考虑，真实反映客流规律
② 动态响应：数据随真实时间自动变化
③ 灵活扩展：可以轻松添加更多时间维度
④ 智能预警：基于时间因素触发客流预警

让演示数据更加真实可信！"
```

---

## 📊 对比优势

### 与静态数据对比

| 特性 | 静态数据 | 时间感知模型 |
|-----|---------|------------|
| **真实性** | ❌ 低 | ✅ 高 |
| **动态性** | ❌ 无 | ✅ 随时间变化 |
| **演示效果** | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **说服力** | ⭐⭐ | ⭐⭐⭐⭐⭐ |

### 与竞品对比

| 维度 | 本项目 | 其他参赛作品 |
|:----|:-----:|:----------:|
| 时间感知 | ✅ 四维模型 | ❌ 静态数据 |
| 演示效果 | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| 真实性 | ⭐⭐⭐⭐⭐ | ⭐⭐ |

---

## 🚀 扩展应用

### 可添加的时间维度

1. **节假日系数**
   - 春节、国庆: 1.5倍
   - 清明、端午: 1.2倍

2. **活动系数**
   - 景区举办活动: 1.3倍
   - 促销活动: 1.2倍

3. **事件系数**
   - 负面事件: 0.7倍
   - 正面宣传: 1.2倍

### 扩展示例

```python
def get_extended_factor(self, is_holiday: bool = False, has_event: bool = False) -> float:
    base = self.get_combined_factor("晴")

    if is_holiday:
        base *= 1.5  # 节假日
    if has_event:
        base *= 1.3  # 活动期间

    return base
```

---

## 📈 数据可视化

### 时间系数曲线图

```
小时系数变化曲线（24小时）

系数
1.5 |       ╱───╲
    |      ╱     ╲
1.3 |     ╱       ╲    ╱─╲
    |    ╱         ╲  ╱   ╲
1.0 |───╱           ╲╱     ╲───
    |  8            16     21
0.5 |
    +─────────────────────────→ 时间
      0    8   12   16   20   24
```

### 综合系数对比

```
场景对比：

工作日早上 9点: 1.1 × 1.0 × 1.0 × 1.0 = 1.1
周六下午2点:   1.4 × 1.35 × 1.0 × 1.0 = 1.89
暑期周六雨天:  1.4 × 1.35 × 1.25 × 0.5 = 1.18
```

---

## 🎓 算法优势

### 1. 科学性

- 基于真实客流规律设计
- 四个维度相互独立，符合概率论

### 2. 灵活性

- 每个维度可单独调整
- 支持添加新的时间维度

### 3. 可解释性

- 每个系数都有明确的业务含义
- 便于理解和调试

### 4. 实时性

- 基于系统时间自动计算
- 无需手动更新数据

---

## 📝 代码示例

### 完整使用流程

```python
# 1. 初始化时间感知生成器
time_gen = TimeAwareGenerator()

# 2. 获取当前天气
weather = time_gen.get_current_weather()
# {'temperature': 25, 'condition': '晴', 'humidity': 50, 'wind': '3级'}

# 3. 计算综合时间系数
factor = time_gen.get_combined_factor(weather['condition'])
# 1.89 (周六下午2点，晴天)

# 4. 计算实时客流量
base = 2000
current = int(base * factor)  # 3780人

# 5. 判断是否为高峰时段
is_peak = time_gen.is_peak_hour()  # True

# 6. 生成时间提示
tips = time_gen._get_time_tips()
# "今天是周末，当前为高峰时段，建议错峰游览"
```

---

## 🏆 总结

### 核心价值

1. **真实性**: ⭐⭐⭐⭐⭐
   - 四维模型高度还原真实客流规律

2. **动态性**: ⭐⭐⭐⭐⭐
   - 数据随时间实时变化

3. **演示性**: ⭐⭐⭐⭐⭐
   - 调整时间即可看到数据变化

4. **扩展性**: ⭐⭐⭐⭐
   - 可添加更多时间维度

### 演示建议

- **最佳演示方式**: 调整系统时间，观察客流量变化
- **演示时长**: 2-3分钟
- **关键话术**: "数据随真实时间动态变化"
- **视觉冲击**: 时间系数曲线图 + 实时数字跳动

---

**文档版本**: v1.0
**最后更新**: 2026-01-02
**作者**: 山西文旅智能体团队
