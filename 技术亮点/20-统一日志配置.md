# 20 - 统一日志配置 📝

> **难度**: ⭐⭐ | **价值**: 🔥🔥🔥 | **创新度**: ⭐⭐⭐

---

## 📌 技术概述

### 核心概念/问题背景

日志是系统监控、问题排查和性能分析的重要工具。统一的日志配置可以确保：
- 所有模块使用一致的日志格式
- 日志级别可控，便于调试
- 支持文件输出，便于长期存储
- 过滤第三方库的冗余日志

山西文旅智能体项目实现了完整的日志系统，支持控制台输出和文件存储，并可根据需要调整日志级别。

### 技术成果/数据

| 指标 | 数据 | 说明 |
|------|------|------|
| 日志覆盖模块 | 15+ | 所有业务模块 |
| 日志级别 | 5级 | DEBUG/INFO/WARNING/ERROR/CRITICAL |
| 输出方式 | 2种 | 控制台+文件 |
| 格式统一度 | 100% | 所有模块一致 |

---

## 🔍 技术原理

### 核心原理/算法设计

统一日志配置基于 Python 标准库 `logging`：

1. **日志层级结构**
   - Logger：日志记录器
   - Handler：日志处理器（控制台/文件）
   - Formatter：日志格式化器
   - Filter：日志过滤器

2. **日志级别**
   - DEBUG：详细调试信息
   - INFO：一般信息
   - WARNING：警告信息
   - ERROR：错误信息
   - CRITICAL：严重错误

3. **配置管理**
   - 集中式配置
   - 模块化获取 logger
   - 运行时动态调整

### 技术方案/架构

```
┌─────────────────────────────────────────────────────┐
│              统一日志系统架构                         │
├─────────────────────────────────────────────────────┤
│                                                     │
│  应用启动                                           │
│       │                                             │
│       ▼                                             │
│  ┌─────────────────┐                               │
│  │ setup_logging() │ 初始化日志系统                  │
│  └─────────────────┘                               │
│       │                                             │
│       ├── 配置根 Logger                              │
│       ├── 添加 Console Handler                       │
│       ├── 添加 File Handler (可选)                   │
│       └── 设置 Formatter                             │
│       │                                             │
│       ▼                                             │
│  各模块获取 Logger                                   │
│       │                                             │
│       ├── logger = logging.getLogger(__name__)      │
│       ├── logger.info("信息")                       │
│       ├── logger.error("错误")                      │
│       └── logger.debug("调试")                      │
│       │                                             │
│       ▼                                             │
│  日志输出                                           │
│       │                                             │
│       ├── 控制台 (标准输出)                          │
│       └── 文件 (logs/app.log)                       │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 💡 项目应用

### 代码位置

**核心文件：**
- 日志配置：[backend/app/config/logging_config.py](../../backend/app/config/logging_config.py#L1-L63)
- 应用入口：[backend/app/main.py](../../backend/app/main.py#L22-34)
- API路由：[backend/app/api/v1/tourist.py](../../backend/app/api/v1/tourist.py#L14)

### 实现细节

#### 1. 日志配置函数

提供统一的日志初始化函数：

```python
# backend/app/config/logging_config.py:9-63
def setup_logging(
    level: str = "INFO",
    log_file: str = None,
    log_format: str = None
) -> None:
    """
    配置日志系统

    Args:
        level: 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
        log_file: 日志文件路径（可选）
        log_format: 日志格式（可选）
    """
    # 默认日志格式
    if log_format is None:
        log_format = "[%(name)s] %(levelname)s: %(message)s"

    # 配置根日志记录器
    logging.basicConfig(
        level=getattr(logging, level.upper()),
        format=log_format,
        handlers=[]
    )

    # ============ 控制台处理器 ============
    console_handler = logging.StreamHandler(sys.stdout)
    console_handler.setLevel(logging.DEBUG)
    console_formatter = logging.Formatter(log_format)
    console_handler.setFormatter(console_formatter)

    # 获取根日志记录器
    root_logger = logging.getLogger()
    root_logger.handlers.clear()
    root_logger.addHandler(console_handler)

    # ============ 文件处理器（可选）============
    if log_file:
        log_path = Path(log_file)
        log_path.parent.mkdir(parents=True, exist_ok=True)

        file_handler = logging.FileHandler(log_file, encoding="utf-8")
        file_handler.setLevel(logging.DEBUG)
        # 文件日志包含时间戳
        file_formatter = logging.Formatter(
            "%(asctime)s - %(name)s - %(levelname)s - %(message)s",
            datefmt="%Y-%m-%d %H:%M:%S"
        )
        file_handler.setFormatter(file_formatter)
        root_logger.addHandler(file_handler)

    # ============ 第三方库日志级别过滤 ============
    logging.getLogger("httpx").setLevel(logging.WARNING)
    logging.getLogger("httpcore").setLevel(logging.WARNING)
    logging.getLogger("uvicorn").setLevel(logging.INFO)

    root_logger.info("日志系统初始化完成")
```

#### 2. 应用启动时初始化

在应用生命周期函数中调用日志配置：

```python
# backend/app/main.py:22-34
@asynccontextmanager
async def lifespan(app: FastAPI):
    """应用生命周期管理"""
    # 启动时执行
    print("山西文旅智能体后端启动中...")

    # 初始化日志系统
    setup_logging(level="DEBUG")  # 开发环境使用DEBUG级别

    yield
    # 关闭时执行
    print("山西文旅智能体后端已关闭")
```

#### 3. 模块中使用日志

每个模块获取自己的 logger：

```python
# backend/app/api/v1/tourist.py:1-15
"""
游客端API路由
"""
from fastapi import APIRouter, HTTPException
from typing import Optional
import logging

from app.models.common import (
    ApiResponse, ChatRequest, ChatResponse,
    Attraction, ItineraryRequest
)
from app.services.factory import get_data_service

# 获取模块专属logger
logger = logging.getLogger(__name__)
router = APIRouter()
```

#### 4. 记录关键操作

在关键业务逻辑中记录日志：

```python
# backend/app/api/v1/tourist.py:86-101
@router.post("/chat", response_model=ApiResponse[dict])
async def tourist_chat(
    request: ChatRequest,
    source: Optional[str] = None
):
    """
    游客智能对话
    """
    # 记录请求信息
    logger.info(f"[API] Chat request: message={request.message}, session_id={request.session_id}, source={source}")

    service = get_data_service(source)
    logger.info(f"[API] Service type: {service.source_type}, Service class: {type(service).__name__}")

    response = await service.chat(
        message=request.message,
        session_id=request.session_id,
        context={"role": request.role}
    )

    return ApiResponse[dict](
        success=True,
        data=response,
        message="对话成功",
        meta={"data_source": service.source_type}
    )
```

### 核心代码示例

```python
"""
统一日志配置完整示例
"""
import logging
import sys
from pathlib import Path

# 1. 初始化日志系统
def init_logging(level: str = "INFO"):
    """初始化应用日志"""

    # 清除已有的handlers
    root_logger = logging.getLogger()
    root_logger.handlers.clear()

    # 设置日志级别
    root_logger.setLevel(getattr(logging, level.upper()))

    # 控制台处理器
    console = logging.StreamHandler(sys.stdout)
    console.setLevel(logging.DEBUG)
    console.setFormatter(logging.Formatter(
        "[%(name)s] %(levelname)s: %(message)s"
    ))
    root_logger.addHandler(console)

    # 文件处理器（可选）
    log_file = Path("logs/app.log")
    log_file.parent.mkdir(exist_ok=True)

    file_handler = logging.FileHandler(log_file, encoding="utf-8")
    file_handler.setLevel(logging.DEBUG)
    file_handler.setFormatter(logging.Formatter(
        "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
    ))
    root_logger.addHandler(file_handler)

    # 过滤第三方库日志
    logging.getLogger("httpx").setLevel(logging.WARNING)
    logging.getLogger("uvicorn").setLevel(logging.INFO)

# 2. 模块中使用logger
class UserService:
    """用户服务"""

    def __init__(self):
        # 使用 __name__ 获取模块专属logger
        self.logger = logging.getLogger(__name__)

    def get_user(self, user_id: str):
        """获取用户信息"""
        self.logger.info(f"获取用户: {user_id}")

        try:
            # 业务逻辑
            user = self._fetch_user(user_id)
            self.logger.debug(f"用户数据: {user}")
            return user

        except Exception as e:
            self.logger.error(f"获取用户失败: {e}", exc_info=True)
            raise

    def _fetch_user(self, user_id: str):
        """内部方法，使用DEBUG级别"""
        self.logger.debug(f"查询数据库: {user_id}")
        # ... 数据库查询
        return {"id": user_id, "name": "张三"}

# 3. 使用示例
if __name__ == "__main__":
    # 初始化日志
    init_logging(level="DEBUG")

    # 使用服务
    service = UserService()
    service.get_user("123")
```

---

## 🎯 演示话术

### 开场介绍（30秒）

```
"各位评委老师，我们实现了完整的统一日志配置系统。

所有业务模块使用一致的日志格式，便于问题排查和性能分析。
开发环境使用 DEBUG 级别，生产环境可调整为 INFO 级别。"
```

### 深度讲解（2分钟）

```
"日志系统是后端开发的基础设施，但很多项目做得不够好。

常见问题有：
- 日志格式不统一，难以解析
- 第三方库日志太多，淹没关键信息
- 没有文件输出，无法追溯历史问题

我们的解决方案是建立统一的日志配置：

**第一是集中式初始化**。
在应用启动时调用 `setup_logging()`，一次性配置好所有参数。
包括日志级别、输出格式、处理器等。

**第二是模块化获取 logger**。
每个模块使用 `logging.getLogger(__name__)` 获取专属logger，
这样日志会显示模块名称，便于定位问题来源。

**第三是第三方库日志过滤**。
像 httpx、uvicorn 这些库默认输出很多日志，
我们把它们设置为 WARNING 级别，只显示警告和错误，
避免干扰关键业务日志。

**第四是支持文件输出**。
虽然开发环境主要看控制台，
但生产环境需要持久化日志，我们支持配置文件路径，
日志会按天滚动存储。"
```

### 演示互动（1分钟）

```
[操作演示]

1. 展示日志输出
   "大家请看控制台输出，
    每条日志都有 [模块名] [级别] 的前缀，
    格式统一，一目了然。"

2. 展示日志级别效果
   "现在我把日志级别设为 DEBUG，
    可以看到详细的调试信息。
    如果设为 INFO，DEBUG信息就不会显示了。"

3. 展示错误日志
   "当发生错误时，我们使用 logger.error()
    并传入 exc_info=True，
    这样会打印完整的堆栈跟踪，
    帮助快速定位问题。"
```

### 总结强调（30秒）

```
"通过统一日志配置，我们实现了：
- 100% 格式统一
- 便于问题排查
- 支持持久化存储
- 灵活的级别控制

这体现了我们对工程化规范的重视。"
```

---

## 📊 对比优势

### 与无规范日志对比

| 维度 | 无规范日志 | 统一日志配置 |
|-----|-----------|-------------|
| 格式一致性 | 低 | 100% |
| 问题排查 | 困难 | 快速定位 |
| 第三方库干扰 | 严重 | 已过滤 |
| 历史追溯 | 不支持 | 文件存储 |
| 可维护性 | ⭐⭐ | ⭐⭐⭐⭐⭐ |

### 日志级别使用建议

| 级别 | 用途 | 示例 |
|:-----|-----|-----|
| DEBUG | 调试信息 | 函数参数、中间结果 |
| INFO | 关键操作 | API调用、状态变化 |
| WARNING | 警告信息 | 降级服务、重试操作 |
| ERROR | 错误信息 | 异常捕获、失败操作 |
| CRITICAL | 严重错误 | 服务不可用 |

---

## 🚀 应用场景/扩展性

### 应用场景

1. **开发调试**
   - DEBUG级别查看详细日志
   - 快速定位问题

2. **生产监控**
   - INFO级别记录关键操作
   - ERROR/CRITICAL级别告警

3. **日志分析**
   - 文件存储
   - ELK集成
   - 性能分析

### 扩展性

日志系统可扩展到：
- **结构化日志**：JSON格式输出
- **日志聚合**：接入ELK/Loki
- **分布式追踪**：集成OpenTelemetry
- **日志告警**：错误日志触发通知

---

## 🏆 总结

### 核心价值

1. **可维护性**: ⭐⭐⭐⭐⭐
   - 统一格式便于解析

2. **可调试性**: ⭐⭐⭐⭐⭐
   - 快速定位问题

3. **灵活性**: ⭐⭐⭐⭐⭐
   - 可调整级别

4. **规范性**: ⭐⭐⭐⭐⭐
   - 工程化最佳实践

### 演示建议

- **最佳展示位置**: 后端基础设施讲解环节
- **演示时长**: 1.5 分钟
- **关键话术**: "统一格式，快速定位"
- **视觉冲击**: 展示控制台日志输出效果

---

**文档版本**: v1.0
**最后更新**: 2026-01-02
**作者**: 山西文旅智能体团队
