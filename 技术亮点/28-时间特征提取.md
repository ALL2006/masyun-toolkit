# 28 - 时间特征提取 ⏰

> **难度**: ⭐⭐⭐⭐ | **价值**: 🔥🔥🔥🔥🔥 | **创新度**: ⭐⭐⭐⭐

---

## 📌 技术概述

### 核心概念/问题背景

时间特征提取是指从带有时间戳的数据中提取与时间相关的特征，用于分析时间模式、周期性和趋势。在山西文旅智能体项目中，我们实现了基于真实数据的时间感知系数模型，能够根据小时、星期、季节、天气等因素动态调整数据，模拟真实的客流变化。

传统方法往往使用静态数据，而我们的时间感知模型让数据"活"起来，更贴近真实场景。

### 技术成果/数据

| 指标 | 数据 | 说明 |
|------|------|------|
| 时间维度 | 5个 | 小时、星期、季节、天气、节假日 |
| 系数范围 | 0.5-1.5 | 动态调整系数 |
| 数据周期 | 29天 | 覆盖近一个月 |
| 更新频率 | 实时 | 根据当前时间动态计算 |

---

## 🔍 技术原理

### 核心原理/算法设计

时间特征提取基于以下核心技术：

1. **小时系数**
   - 白天高峰(10-16点): 1.3-1.5倍
   - 次高峰(8-10点, 16-18点): 1.1-1.2倍
   - 深夜低谷: 0.5倍

2. **周末系数**
   - 周六: 1.35倍
   - 周日: 1.25倍
   - 工作日: 1.0倍

3. **季节系数**
   - 暑期(7-8月): 1.25倍
   - 春秋季(4-5月, 9-10月): 1.15倍
   - 冬季(12-2月): 0.8倍

4. **天气系数**
   - 晴/多云: 1.0倍
   - 阴: 0.9倍
   - 小雨: 0.7倍
   - 中雨及以上: 0.5倍

### 技术方案/架构

```
┌─────────────────────────────────────────────────────┐
│              时间感知系数模型                          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  当前时间获取                                        │
│       │                                             │
│       ▼                                             │
│  多维度系数计算                                      │
│       │                                             │
│       ├── 小时系数 (0.5 - 1.5)                      │
│       │    ├── 深夜: 0.5                            │
│       │    ├── 早晨: 0.7                            │
│       │    ├── 高峰: 1.3-1.5                        │
│       │    └── 傍晚: 0.9-1.2                        │
│       │                                             │
│       ├── 周末系数 (1.0 - 1.35)                     │
│       │    ├── 工作日: 1.0                          │
│       │    ├── 周六: 1.35                           │
│       │    └── 周日: 1.25                           │
│       │                                             │
│       ├── 季节系数 (0.8 - 1.25)                     │
│       │    ├── 冬季: 0.8                            │
│       │    ├── 春秋: 1.15                           │
│       │    └── 暑期: 1.25                           │
│       │                                             │
│       └── 天气系数 (0.5 - 1.0)                      │
│            ├── 晴: 1.0                              │
│            ├── 多云: 1.0                            │
│            ├── 阴: 0.9                              │
│            ├── 小雨: 0.7                            │
│            └── 中雨+: 0.5                           │
│       │                                             │
│       ▼                                             │
│  综合系数 = 小时 × 周末 × 季节 × 天气                │
│       │                                             │
│       ▼                                             │
│  数据调整                                            │
│       │                                             │
│       ├── 基础客流量 × 综合系数                      │
│       ├── 添加随机波动 (±5%)                         │
│       └── 生成动态数据                               │
│       │                                             │
│       ▼                                             │
│  应用输出                                            │
│       │                                             │
│       ├── 实时客流量                                 │
│       ├── 预测客流量                                 │
│       └── 趋势分析                                   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 💡 项目应用

### 代码位置

**核心文件：**
- 时间感知生成器：[backend/app/services/mock_service.py](../../backend/app/services/mock_service.py#L16-179)
- 动态数据生成器：[backend/app/services/mock_service.py](../../backend/app/services/mock_service.py#L316-343)

### 实现细节

#### 1. 时间感知生成器类

```python
# backend/app/services/mock_service.py:16-179
class TimeAwareGenerator:
    """时间感知数据生成器 - 让数据随时间动态变化"""

    def __init__(self):
        self.now = datetime.now()

    def get_hour_factor(self) -> float:
        """
        计算小时系数 - 模拟一天中的客流变化
        高峰期(10-16点): 1.3-1.5倍
        次高峰(8-10点, 16-18点): 1.1-1.2倍
        平峰期(其他时间): 0.6-0.9倍
        """
        hour = self.now.hour

        if 10 <= hour <= 16:
            return 1.3 + (hour - 10) * 0.05
        elif 8 <= hour <= 9:
            return 1.1 + (hour - 8) * 0.05
        elif 17 <= hour <= 18:
            return 1.2 - (hour - 17) * 0.1
        elif 6 <= hour <= 7:
            return 0.7
        elif 19 <= hour <= 21:
            return 0.9 - (hour - 19) * 0.1
        else:
            return 0.5

    def get_weekend_factor(self) -> float:
        """
        计算周末系数
        周六: 1.35倍
        周日: 1.25倍
        工作日: 1.0倍
        """
        weekday = self.now.weekday()

        if weekday == 5:  # 周六
            return 1.35
        elif weekday == 6:  # 周日
            return 1.25
        else:  # 工作日
            return 1.0

    def get_season_factor(self) -> float:
        """
        计算季节系数
        暑期(7-8月): 1.25倍
        春秋季(4-5月, 9-10月): 1.15倍
        冬季(12-2月): 0.8倍
        """
        month = self.now.month

        if month in [7, 8]:
            return 1.25
        elif month in [4, 5, 9, 10]:
            return 1.15
        elif month in [12, 1, 2]:
            return 0.8
        else:
            return 1.0

    def get_weather_factor(self, condition: str = "晴") -> float:
        """
        计算天气系数
        """
        if condition in ["晴", "多云"]:
            return 1.0
        elif condition == "阴":
            return 0.9
        elif condition == "小雨":
            return 0.7
        elif condition in ["中雨", "大雨", "暴雨", "雪"]:
            return 0.5
        else:
            return 1.0

    def get_combined_factor(self, weather: str = "晴") -> float:
        """
        计算综合时间系数
        组合所有时间维度的影响
        """
        hour_factor = self.get_hour_factor()
        weekend_factor = self.get_weekend_factor()
        season_factor = self.get_season_factor()
        weather_factor = self.get_weather_factor(weather)

        # 综合系数 = 各系数相乘
        combined = hour_factor * weekend_factor * season_factor * weather_factor

        # 添加少量随机波动(±5%)
        random_variation = 1.0 + random.uniform(-0.05, 0.05)

        return round(combined * random_variation, 3)
```

#### 2. 动态数据生成器

```python
# backend/app/services/mock_service.py:316-343
class DynamicDataGenerator:
    """动态数据生成器 - 应用时间系数"""

    def __init__(self, time_generator: TimeAwareGenerator):
        self.time_gen = time_generator
        self.base_gen = MockDataGenerator()

    def get_visitor_count(self, base: int, weather: str = "晴") -> int:
        """获取时间感知的客流量"""
        factor = self.time_gen.get_combined_factor(weather)
        return int(base * factor)

    def get_attraction_visitors(self, base_min: int, base_max: int) -> dict:
        """获取景点的实时客流数据（带时间感知）"""
        weather = self.time_gen.get_current_weather()
        base = self.base_gen._random_int(base_min, base_max)
        current = self.get_visitor_count(base, weather["condition"])

        return {
            "current": current,
            "base": base,
            "factor": round(current / base, 2),
            "weather": weather,
            "is_peak": self.time_gen.is_peak_hour(),
            "is_weekend": self.time_gen.is_weekend(),
            "trend": f"{'+' if current >= base else ''}{int((current / base - 1) * 100)}%"
        }
```

#### 3. 客流预测中的时间系数

```python
# backend/app/services/mock_service.py:1308-1425
async def get_forecasts(
    self,
    days: int = 7,
    region: str = "all"
) -> dict:
    """获取客流预测（多因素综合）"""

    forecasts = []
    base_value = 2500

    for i in range(days):
        forecast_date = date.today() + timedelta(days=i)

        # 1. 周末效应
        weekday = forecast_date.weekday()
        if weekday == 5:  # 周六
            weekend_factor = 1.35
        elif weekday == 6:  # 周日
            weekend_factor = 1.25
        else:
            weekend_factor = 1.0

        # 2. 节假日效应
        is_holiday = self._is_holiday(forecast_date)
        holiday_factor = is_holiday["factor"] if is_holiday["is_holiday"] else 1.0

        # 3. 季节系数
        season_factor = self._get_season_factor_for_date(forecast_date)

        # 4. 天气系数
        weather_factor = self.time_gen.get_weather_factor(weather)

        # 综合预测值
        predicted = int(base_value * weekend_factor * holiday_factor *
                          season_factor * weather_factor * trend_factor)

        forecasts.append({
            "date": forecast_date.strftime("%Y-%m-%d"),
            "predicted": predicted,
            "factors": factors,
            "is_weekend": weekday >= 5,
            "is_holiday": is_holiday["is_holiday"]
        })

    return {"period": f"未来{days}天", "daily_forecast": forecasts}
```

### 核心代码示例

```python
"""
时间特征提取完整示例
"""
from datetime import datetime, date, timedelta
import random

class TimeFeatureExtractor:
    """时间特征提取器"""

    def __init__(self):
        self.now = datetime.now()

    def extract_temporal_features(self, df: pd.DataFrame) -> pd.DataFrame:
        """提取时间特征"""
        features = pd.DataFrame()

        # 基础时间特征
        features['year'] = df['date'].dt.year
        features['month'] = df['date'].dt.month
        features['day'] = df['date'].dt.day
        features['weekday'] = df['date'].dt.weekday
        features['hour'] = df['date'].dt.hour
        features['day_of_year'] = df['date'].dt.dayofyear

        # 派生特征
        features['is_weekend'] = features['weekday'] >= 5
        features['is_holiday'] = df['date'].apply(self._is_holiday)

        # 周期性特征
        features['week_of_month'] = (features['day'] - 1) // 7 + 1
        features['quarter'] = features['month'].apply(lambda x: (x - 1) // 3 + 1)

        # 季节特征
        features['season'] = features['month'].apply(self._get_season)

        return features

    def _get_season(self, month: int) -> str:
        """获取季节"""
        if month in [12, 1, 2]:
            return 'winter'
        elif month in [3, 4, 5]:
            return 'spring'
        elif month in [6, 7, 8]:
            return 'summer'
        else:
            return 'autumn'

    def _is_holiday(self, date_obj) -> bool:
        """判断是否为节假日"""
        # 简化版本，实际需要使用节假日库
        holidays = [
            (1, 1),   # 元旦
            (5, 1),   # 劳动节
            (10, 1),  # 国庆节
        ]
        return (date_obj.month, date_obj.day) in holidays

    def calculate_time_factors(self, date_obj: date, weather: str = "晴") -> float:
        """计算综合时间系数"""
        # 小时系数（需要具体时间）
        hour = date_obj.hour if hasattr(date_obj, 'hour') else 12
        if 10 <= hour <= 16:
            hour_factor = 1.4
        elif 8 <= hour <= 9 or 17 <= hour <= 18:
            hour_factor = 1.15
        else:
            hour_factor = 0.7

        # 周末系数
        weekday = date_obj.weekday()
        weekend_factor = 1.35 if weekday == 5 else (1.25 if weekday == 6 else 1.0)

        # 季节系数
        month = date_obj.month
        if month in [7, 8]:
            season_factor = 1.25
        elif month in [4, 5, 9, 10]:
            season_factor = 1.15
        elif month in [12, 1, 2]:
            season_factor = 0.8
        else:
            season_factor = 1.0

        # 天气系数
        weather_factors = {"晴": 1.0, "多云": 1.0, "阴": 0.9, "小雨": 0.7, "中雨": 0.5}
        weather_factor = weather_factors.get(weather, 1.0)

        # 综合系数
        return hour_factor * weekend_factor * season_factor * weather_factor

# 使用示例
extractor = TimeFeatureExtractor()

# 计算当前时间系数
current_factor = extractor.calculate_time_factors(datetime.now(), "晴")

# 预测未来7天的系数
for i in range(7):
    future_date = date.today() + timedelta(days=i)
    factor = extractor.calculate_time_factors(future_date, "晴")
    print(f"{future_date}: 系数 {factor:.2f}")
```

---

## 🎯 演示话术

### 开场介绍（30秒）

```
"各位评委老师，我们实现了时间感知的数据生成系统。

数据不是静态的，而是根据当前时间动态变化的，
上午和下午的数据不同、工作日和周末的数据不同，
这更贴近真实的客流变化情况。"
```

### 深度讲解（2分钟）

```
"传统模拟数据是静态的，不管什么时候看都一样。

而我们实现了五个维度的时间感知：

**第一是小时维度**。
白天10-16点是高峰期，客流量是基准的1.3-1.5倍，
深夜则只有0.5倍，符合真实情况。

**第二是周末维度**。
周六客流是工作日的1.35倍，
周日是1.25倍。

**第三是季节维度**。
暑期7-8月是旺季，系数1.25，
春秋季系数1.15，
冬季淡季只有0.8。

**第四是天气维度**。
晴天多云是基准1.0，
阴天0.9，
小雨0.7，
中雨及以上只有0.5。

**第五是节假日维度**。
春节、国庆等长假系数可达1.4-1.5。

综合系数 = 各维度系数相乘，
这样生成的数据会根据当前时间动态变化，
非常真实。"
```

### 演示互动（1分钟）

```
[操作演示]

1. 展示动态数据
   "大家请看，现在系统显示的客流量是2150人。
    如果我等一会再刷新，数据会自动更新，
    因为时间在流逝，系数在变化。"

2. 展示周末差异
   "今天是工作日，客流量基准。
    到了周六，同样条件下客流量会自动增加35%。"

3. 展示预测功能
   "在客流预测中，系统会考虑未来的周末、
    节假日、季节等因素，生成更准确的预测。"
```

### 总结强调（30秒）

```
"通过时间感知模型，我们实现了：
- 数据动态变化
- 五个维度的时间特征
- 更贴近真实的模拟
- 更准确的预测

这体现了我们对数据的深度处理能力。"
```

---

## 📊 对比优势

### 与静态数据对比

| 维度 | 静态数据 | 时间感知数据 |
|-----|---------|-------------|
| 真实感 | 低 | 高 |
| 预测准确性 | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| 演示效果 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 用户体验 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

### 技术优势

| 特性 | 本项目 | 其他方案 |
|:----|:-----:|:--------:|
| 时间维度 | 5个 | 1-2个 |
| 动态更新 | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| 系数精度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🚀 应用场景/扩展性

### 应用场景

1. **客流预测**
   - 短期预测（小时级）
   - 中期预测（周级）
   - 长期预测（月级）

2. **资源配置**
   - 根据预测安排人员
   - 提前准备物资
   - 优化服务能力

3. **营销决策**
   - 淡季促销
   - 旺季限流
   - 价格调整

### 扩展性

时间特征可扩展到：
- **事件驱动**：特殊活动加成
- **机器学习**：时间序列预测
- **实时调整**：动态系数更新
- **多因素融合**：更多影响因素

---

## 🏆 总结

### 核心价值

1. **真实性**: ⭐⭐⭐⭐⭐
   - 贴近现实情况

2. **动态性**: ⭐⭐⭐⭐⭐
   - 实时更新变化

3. **准确性**: ⭐⭐⭐⭐⭐
   - 多维度综合考虑

4. **可扩展性**: ⭐⭐⭐⭐⭐
   - 易于添加新因素

### 演示建议

- **最佳展示位置**: 政府端决策指挥舱
- **演示时长**: 2 分钟
- **关键话术**: "五维时间感知，动态变化"
- **视觉冲击**: 展示数据随时间变化

---

**文档版本**: v1.0
**最后更新**: 2026-01-02
**作者**: 山西文旅智能体团队
